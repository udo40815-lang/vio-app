import React from 'react';

function MainLayout({ children, dark }) {
  const bg = dark ? '#0B1020' : '#F8FAFC';
  return <div className="min-h-screen" style={{ background: bg }}>{children}</div>;
}

export default MainLayout;